1. To install node run the following command in your termnal : 
sudo apt install nodejs

Verify the installation using the following commands : 
node -v or node –version

2. To install angular cli run the following command : 
npm install -g @angular/cli

3. Clone the repository

4. Run the following command to install node packages : 
npm install

5. To start angular server at port 4200, run the following command : 
ng serve
